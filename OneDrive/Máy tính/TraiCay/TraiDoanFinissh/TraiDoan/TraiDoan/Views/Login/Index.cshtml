@model TraiDoan.Models.User

@{
    //if (Session !=null)
    //{
    //    Response.Redirect("Register");
    //}
}


<!DOCTYPE html>
<html>
<head>
    <title>Animated Login Form</title>
    <link rel="stylesheet" type="text/css" href="~/Content/login/css/style.css">
    <link href="https://fonts.googleapis.com/css?family=Poppins:600&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a81368914c.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <img class="wave" src="~/Content/login/img/wave.png">
    <div class="container">
        <div class="img">
            <img src="~/Content/login/img/bg.svg">
        </div>
        <div id="form-login" class="login-content">
            @using (Html.BeginForm("Authen", "Login", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <img src="~/Content/login/img/avatar.svg">
                <h2 class="title">Welcome</h2>

                <div class="input-div one">

                    <div class="i">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="div">
                        @Html.LabelFor(m => m.Email)
                        @Html.TextBoxFor(m => m.Email)
                    </div>
                </div>
                <div class="input-div pass">
                    <div class="i">
                        <i class="fas fa-lock"></i>
                    </div>
                    <div class="div">
                        @Html.LabelFor(m => m.Password)
                        @Html.PasswordFor(m => m.Password)
                    </div>
                </div>




                @Html.ValidationMessageFor(m => m.Email)

                @Html.ValidationMessageFor(m => m.Password)
                @Html.DisplayFor(m => m.LoginErrorMessage)



                <input class="btn btn-primary" type="submit" value="Login" />
                <input class="btn btn-primary" type="reset" value="Reset" />
                <a class="btn btn-primary" style="text-align: CENTER;" href="@Url.Action("Register","Login")">Register</a>
            }
        </div>
    </div>
</body>
</html>